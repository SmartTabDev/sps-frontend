import { styled } from '@mui/system';

const AdvancedFilterRow = styled('div')`
  grid-template-columns: 1fr 1fr;
  display: grid;
  gap: 30px;
  margin-top: 30px;
`;

export default AdvancedFilterRow;
